ДЗ.
Задание 1:
- написать программу мини-калькулятор, которая выводит на экран пользователя alert с результатом математической операции,
  примененной к двум числам, введенным при помощи двух prompt.

Задание 2:
- написать программу, которая проделывает следующие операции:
  запрашивает у пользователя число,
  потом последовательно задает вопросы "Сколько отнять/прибавить/умножить/разделить от предыдущего результата?".
  По окончании вывести пользователю алерт, содержащий формулу и результат (напр., 6 + 10 - 5 = 11).

Задание 3 *:
- даны две переменных a и b. Как, без сложных манипуляций, можно поменять их значения местами?

Ответ:
b = [a, a = b][0];

Смотрим на порядок выполнения операций.
1. Значение переменной a заносится во вновь созданный массив на позицию 0.
2. Переменной а присваивается значение переменной b.
3. Поскольку операция присвоения значения в JavaScript возвращает значение, то таким образом значение переменных a и b(оно на данный момент одинаковое и равно первоначальному значению b) заносится в массив под индексом 1. И теперь переменная a имеет значение переменной b, а в массиве под индексом 0 содержится первоначальное значение a. Здесь надо сказать, что заносить новое значение a в массив было необязательно, это сделано только для того, чтобы уместить все в одно выражение, тут главным было присвоение a нового значения.
4. Нулевой элемент массива(а он имеет первоначальное значение a) передается переменной b. 